import SwiftUI

struct SavedList: View {
    
    var body: some View {
        VStack{
            Text("Mi lista de guardados").font(.title)
        }
        .navigationTitle("Mis guardados")
    }
}
